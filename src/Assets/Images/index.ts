import defaultImage from "./cocktail.png";
import tequila_sunrise from "./tequila_sunrise.png";

export const images: { [key: string]: any } = {
  default: defaultImage,
  tequila_sunrise: tequila_sunrise,
};
